# Prompt: 审查输出是否符合 lailai 风格

复制以下内容给 AI，并附上待审查的输出。

---

请审查下面这段输出是否符合 lailai 风格，用 lailai.skill 的规则与评分标准。

步骤：

1. **定位类型**：判断属于哪类（聊天 / 写作 / Docusaurus / C++ / 设计 / README / 决策）。
2. **对照规则**：读对应 `references/` 文件的 Self-review Checklist 与 `profile/`，逐条核对。
3. **查 AI 腔**：对照 `ai-tone-blacklist.md`，列出所有命中。
4. **打分**：用 `evolution/evaluation-rubric.md` 给相关维度 0～5 分，说明扣分点。
5. **给改写**：对每个问题给出具体修改建议或直接改写片段。

要求：

- 具体到句 / 词 / 行，不泛泛说"可以更简洁"。
- 先给总体判断（像 / 不像 + 主要问题），再逐条。
- 指出但不越界：若涉及隐私、对外表态、人格变化，提示需人工确认。

输出格式：总体判断 → 命中的 AI 腔 → 维度评分 → 逐条改写建议。

待审查输出：

【在此粘贴输出】
